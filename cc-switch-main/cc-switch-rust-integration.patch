warning: in the working copy of 'src/commands/query.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/lib.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/main.rs', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'src/services/api/claude.rs', LF will be replaced by CRLF the next time Git touches it
diff --git a/cc-switch-main/src-tauri/src/commands/misc.rs b/cc-switch-main/src-tauri/src/commands/misc.rs
index 4e491b5f..c0dad039 100644
--- a/cc-switch-main/src-tauri/src/commands/misc.rs
+++ b/cc-switch-main/src-tauri/src/commands/misc.rs
@@ -200,7 +200,21 @@ async fn get_single_tool_version_impl(
 
     // 2. 获取远程最新版本
     let latest_version = match tool {
-        "claude" => fetch_npm_latest_version(&client, "@anthropic-ai/claude-code").await,
+        "claude" => {
+            // 检测本地版本类型
+            let version_type = if let Some(ref version) = local_version {
+                // 获取原始版本来检测类型
+                let raw_output = get_raw_version_output(tool).await.unwrap_or_default();
+                detect_claude_version_type(&raw_output)
+            } else {
+                "js" // 默认使用JS版本
+            };
+
+            match version_type {
+                "rust" => fetch_github_latest_version(&client, "anthropics/claude-code-rust").await,
+                _ => fetch_npm_latest_version(&client, "@anthropic-ai/claude-code").await,
+            }
+        }
         "codex" => fetch_npm_latest_version(&client, "@openai/codex").await,
         "gemini" => fetch_npm_latest_version(&client, "@google/gemini-cli").await,
         "opencode" => fetch_github_latest_version(&client, "anomalyco/opencode").await,
@@ -262,12 +276,52 @@ async fn fetch_github_latest_version(client: &reqwest::Client, repo: &str) -> Op
 static VERSION_RE: Lazy<Regex> =
     Lazy::new(|| Regex::new(r"\d+\.\d+\.\d+(-[\w.]+)?").expect("Invalid version regex"));
 
-/// 从版本输出中提取纯版本号
-fn extract_version(raw: &str) -> String {
-    VERSION_RE
-        .find(raw)
-        .map(|m| m.as_str().to_string())
-        .unwrap_or_else(|| raw.to_string())
+/// 获取原始版本输出
+async fn get_raw_version_output(tool: &str) -> Option<String> {
+    use std::process::Command;
+
+    #[cfg(target_os = "windows")]
+    let output = {
+        Command::new("cmd")
+            .args(["/C", &format!("{tool} --version")])
+            .creation_flags(CREATE_NO_WINDOW)
+            .output()
+    };
+
+    #[cfg(not(target_os = "windows"))]
+    let output = {
+        Command::new("sh")
+            .arg("-c")
+            .arg(format!("{tool} --version"))
+            .output()
+    };
+
+    match output {
+        Ok(out) => {
+            if out.status.success() {
+                let stdout = String::from_utf8_lossy(&out.stdout).trim().to_string();
+                let stderr = String::from_utf8_lossy(&out.stderr).trim().to_string();
+                let raw = if stdout.is_empty() { &stderr } else { &stdout };
+                if raw.is_empty() {
+                    None
+                } else {
+                    Some(raw.to_string())
+                }
+            } else {
+                None
+            }
+        }
+        Err(_) => None,
+    }
+}
+
+/// 检测Claude版本类型（Rust或JavaScript）
+fn detect_claude_version_type(version_output: &str) -> &'static str {
+    if version_output.contains("claude-code-rs") || version_output.contains("[Rust]") {
+        "rust"
+    } else {
+        "js"
+    }
 }
 
 /// 尝试直接执行命令获取版本
diff --git a/src/commands/query.rs b/src/commands/query.rs
index c234d07a..8cbb1b17 100644
--- a/src/commands/query.rs
+++ b/src/commands/query.rs
@@ -1,22 +1,40 @@
 //! 单次查询命令
-//! 
-//! 这个模块实现了单次查询功能
+//!
+//! 这个模块实现了单次查询功能，使用 QueryEngine 处理查询。
 
 use crate::config::Settings;
 use crate::error::Result;
 use crate::state::AppState;
+use crate::tools;
+use std::sync::Arc;
 
 /// 运行单次查询
 pub async fn run(query: String, settings: Settings, state: AppState) -> Result<()> {
     tracing::info!("Running query: {}", query);
-    
-    // TODO: 实现实际的查询逻辑
-    // 1. 准备消息
-    // 2. 调用 API
-    // 3. 显示响应
-    
-    println!("Query: {}", query);
-    println!("(Query processing not yet implemented)");
-    
+
+    // 初始化工具系统
+    let tool_manager = Arc::new(tools::init().await?);
+
+    // 创建查询引擎
+    let query_engine = crate::query::engine::QueryEngine::new(
+        settings,
+        state,
+        tool_manager,
+    ).await?;
+
+    // 提交查询
+    let result = query_engine.submit_message(&query).await?;
+
+    // 显示结果
+    if let Some(response) = result.response {
+        if let Some(text) = response.text_content() {
+            println!("{}", text);
+        } else {
+            println!("(Received non-text response)");
+        }
+    } else {
+        println!("(No response generated)");
+    }
+
     Ok(())
 }
diff --git a/src/lib.rs b/src/lib.rs
index 83e0a51f..4e1a1704 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -32,6 +32,7 @@ pub mod performance;
 pub mod security;
 pub mod api;
 pub mod plugins;
+pub mod query;
 
 // Re-export commonly used types
 pub use error::{ClaudeError, Result};
diff --git a/src/main.rs b/src/main.rs
index 17ce828f..63213158 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -190,7 +190,7 @@ fn init_logging() -> Result<()> {
 async fn run(cli: Cli) -> Result<()> {
     // Show version if requested
     if let Some(Commands::Version) = cli.command {
-        println!("{} ({})", env!("CARGO_PKG_VERSION"), env!("CARGO_PKG_NAME"));
+        println!("{} ({}) [Rust]", env!("CARGO_PKG_VERSION"), env!("CARGO_PKG_NAME"));
         return Ok(());
     }
     
diff --git a/src/services/api/claude.rs b/src/services/api/claude.rs
index 79a6126e..60a5efad 100644
--- a/src/services/api/claude.rs
+++ b/src/services/api/claude.rs
@@ -1,41 +1,59 @@
 //! Claude API 客户端
-//! 
+//!
 //! 这个模块实现了 Claude API 客户端功能
 
-use crate::error::Result;
+use crate::api::{ApiClient, ApiClientConfig};
+use crate::api::error::ApiError;
+use crate::error::{Result, ClaudeError};
 use serde::{Deserialize, Serialize};
+use std::collections::HashMap;
 
 /// Claude API 客户端
 pub struct ClaudeApi {
     /// API 密钥
     api_key: Option<String>,
-    
+
     /// 基础 URL
     base_url: String,
-    
+
     /// 模型
     model: String,
+
+    /// HTTP 客户端
+    client: ApiClient,
 }
 
 impl ClaudeApi {
     /// 创建新的 Claude API 客户端
     pub fn new() -> Self {
+        let base_url = "https://api.anthropic.com".to_string();
+        let config = ApiClientConfig::default();
+        let client = ApiClient::new(&base_url, config);
+
         Self {
             api_key: None,
-            base_url: "https://api.anthropic.com".to_string(),
+            base_url,
             model: "claude-3-5-sonnet-20241022".to_string(),
+            client,
         }
     }
     
     /// 设置 API 密钥
     pub fn with_api_key(mut self, api_key: String) -> Self {
-        self.api_key = Some(api_key);
+        self.api_key = Some(api_key.clone());
+        self.client = self.client.with_api_key(&api_key);
         self
     }
     
     /// 设置基础 URL
     pub fn with_base_url(mut self, base_url: String) -> Self {
-        self.base_url = base_url;
+        self.base_url = base_url.clone();
+        let config = ApiClientConfig::default();
+        let mut client = ApiClient::new(&base_url, config);
+        if let Some(ref api_key) = self.api_key {
+            client = client.with_api_key(api_key);
+        }
+        self.client = client;
         self
     }
     
@@ -46,9 +64,59 @@ impl ClaudeApi {
     }
     
     /// 发送消息
-    pub async fn send_message(&self, _message: &str) -> Result<String> {
-        // TODO: 实现实际的消息发送逻辑
-        Ok("Response from Claude API".to_string())
+    pub async fn send_message(&self, message: &str) -> Result<String> {
+        // 构建请求
+        let request = ClaudeRequest {
+            model: self.model.clone(),
+            messages: vec![ClaudeMessage {
+                role: "user".to_string(),
+                content: message.to_string(),
+            }],
+            max_tokens: Some(1024),
+        };
+
+        // 构建请求头
+        let mut headers = HashMap::new();
+        headers.insert("Content-Type".to_string(), "application/json".to_string());
+        headers.insert("anthropic-version".to_string(), "2023-06-01".to_string());
+        if let Some(ref api_key) = self.api_key {
+            headers.insert("x-api-key".to_string(), api_key.clone());
+        }
+
+        // 创建API请求
+        let api_request = crate::api::Request::post("/v1/messages", request)
+            .with_headers(headers)
+            .with_auth(false); // 禁用默认的Authorization头
+
+        // 发送请求并处理API错误
+        let response = self.client.send::<ClaudeRequest, ClaudeResponse>(api_request)
+            .await
+            .map_err(|e| {
+                match e {
+                    ApiError::Network(err) => ClaudeError::Network(err),
+                    ApiError::Http { status, message } => ClaudeError::Other(format!("HTTP error {}: {}", status, message)),
+                    ApiError::Timeout => ClaudeError::Other("Request timeout".to_string()),
+                    ApiError::Serialization(err) => ClaudeError::Serialization(err),
+                    ApiError::Compression(err) => ClaudeError::Other(format!("Compression error: {}", err)),
+                    ApiError::Business { code, message } => ClaudeError::Other(format!("Business error [{}]: {}", code, message)),
+                    ApiError::Other(msg) => ClaudeError::Other(msg),
+                }
+            })?;
+
+        // 解析响应
+        match response.body {
+            Some(claude_response) => {
+                // 提取文本内容
+                let text = claude_response.content
+                    .iter()
+                    .filter_map(|c| c.text.as_ref())
+                    .map(|t| t.as_str())
+                    .collect::<Vec<_>>()
+                    .join("\n");
+                Ok(text)
+            },
+            None => Err(crate::error::ClaudeError::Other("Empty response body".to_string()))?,
+        }
     }
     
     /// 获取模型
